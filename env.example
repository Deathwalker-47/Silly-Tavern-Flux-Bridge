# Flux LoRA Bridge Configuration
# At least ONE provider must be configured

# Option 1: HuggingFace ZeroGPU (FREE, unlimited LoRAs) - via Gradio Client
# Your HF Space name (e.g., "username/space-name")
HF_SPACE_NAME=
# HF Token (only if space is private)
HF_TOKEN=

# Option 2: Together AI ($0.02/image, 10 LoRAs max) - via Together SDK
# Get API key from: https://api.together.xyz/settings/api-keys
TOGETHER_API_KEY=

# Option 3: Wavespeed ($0.015/image, 4 LoRAs max) - via REST API
# Get API key from: https://wavespeed.ai/dashboard/api-keys
WAVESPEED_API_KEY=
